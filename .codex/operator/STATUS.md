# Workspace GOAL Recovery Status

- Workspace GOAL: `ACTIVE / UNMET`. A completed KLR implementation cycle is
  terminal failure evidence, not completion of `01. GOAL/GOAL.md`.
- Operator correction: the thread goal was incorrectly marked `complete` after
  925,764 tokens / 3,343 seconds while the Owner outcome and workspace GOAL were
  still unmet. That status is rejected; intermediate tests, artifacts and
  documentation do not close the objective.
- Exact completion gate: the book must simultaneously pass PF after verified
  cost, 2-5 trades per elapsed calendar week on each split, x1.5/x2 cost
  stress, declared exposure/DD limits, independent train/holdout, and the full
  promotion evidence stack. Do not close the operator goal for a compile pass,
  one backtest, or a terminal strategy verdict.
- Latest experiment: `GOAL-BLOCK-E003` completed the required third consecutive
  live-state audit. `DATABENTO_API_KEY` is still absent, no independent Unicorn
  human-label artifact exists, and the registry has zero open hypotheses
  (canonical counts: run `04. Memory/research/validate_candidate_registry.py`;
  do not hard-code counts in this ledger). AI calibration/context
  files do not clear the human gate.
- Operator stop state:
  `BLOCKED_EXTERNAL_INPUT_AFTER_THREE_CONSECUTIVE_GOAL_TURNS`. The workspace
  GOAL remains UNMET and must not be marked complete. Resume the same full
  objective only after the Owner configures the Databento key locally and later
  approves an explicit live USD ceiling, or supplies contract-compliant
  independent human labels. Audit SHA256:
  `4AE44E7D3C646E556191D415A8134BC4DBD51B62A18B5CC94990B5B24E58F17E`.
- Delivery correction is outcome-led: continue the next safe source/compile/
  test/backtest-or-probe/analyze step without Owner micro-prompts. Status updates
  are informational, not approval gates; process changes are justified only by
  a repeated material failure or an immediate verification need.

## KLR / PO3-AMD family

- Authority: operational recovery ledger only; `04. Memory/hot.md` remains the
  canonical live-scope owner.
- Goal: evaluate the Owner-requested KLR build against the existing PO3-AMD
  family evidence, XAUUSD M5 first, with no live execution and no post-hoc
  rescue.
- Acceptance: de-dup and cheap probe; frozen prereg/registry; canonical package;
  non-repaint audit; AlphaFactory compile; D-portable native Model 0; report and
  lifecycle reconciliation; no live execution or post-hoc rescue.
- Current state: `KILL_AT_MODEL0_CADENCE`. Owner correctly reopened native
  proof after the offline kill; `EA_KLR_Scalper.mq5` now exists for audit.
- KLR native evidence: source compiled 0/0 and passed 7/7 tests plus both
  snapshot-bound non-repaint audits. Control `20260716_142720` produced N=4,
  0.02555/week, PF 1.891, net +267.68 USD; USD run `20260716_142900` produced
  N=1, 0.00639/week, net +78.74 USD. Both miss the 2-5/week cadence gate by
  orders of magnitude.
- Latest evidence: old XAU range-sweep Model 0 `20260621_204037` PF `0.45`,
  N `29`; historical S700 PF `0.88`, N `446`; EUR Asian manipulation cost PF
  about `1.08`. PO3 must separate via bias + displacement + MSS + FVG retest.
- Current diagnosis: native funnel 346 raids -> 61 displacement/MSS -> 26 FVG
  -> 5 retests confirms the sparse offline bottleneck. Tiny-sample positive PF
  is not an edge; live cost provenance remains insufficient but cannot rescue
  cadence. C Common stayed metadata-identical; tester/runs are on D.
- Next experiment: none for KLR/PO3 IDs. Require a materially different causal
  information set and fresh Owner-scoped preregistration; no session,
  signal-threshold, RR, year or USD-gate rescue.
- Budget: exhausted for the KLR/PO3 family; retain source/runs audit-only.

## DRAT ONNX + ICT lane

- Goal: build and falsify `EA_DRAT_ONNX_ICT_Hybrid` without rescuing the killed
  Hybrid-Sonic hypothesis.
- Current state: `HYP-DRAT-ONNX-ICT-M15-EUR-001` is
  `KILL_AT_OFFLINE_PROBE`; it stopped before model export/source/compile/MT5.
- Evidence: rules-only PF 0.7642 / -67.75R versus ONNX-gated PF 0.7488 /
  -52.82R; cadence passed but 2024, 2025 and partial 2026 were all negative.
- Storage: raw bars stay in memory; retained artifacts live on D. Only
  run-created disposable C tester/log files may be deleted after D-side
  copy/hash verification; shared history/account/config remain protected.
- Frontier audit: Gold→USDJPY was the last plausible local alternative; its
  Mon/Thu-unfiltered Model 0 had N=931 / ~3.57 tpw but PF 0.97 and net
  -$710.49. No options-implied/OI or signed order-flow dataset exists locally.
- Resume state: Owner registered Databento for the CME EUR/USD options lane and
  workspace-local storage. The `GLBX.MDP3` planner, D-side SDK runtime and
  hidden-input key helper are implemented; 5/5 acquisition contract tests pass.
  Current state is `DATABENTO_ACCOUNT_REGISTERED_API_KEY_NOT_CONFIGURED`:
  metadata/cost planning has not run, zero time-series requests were made and
  no charge was incurred. Threshold/session/RR/model rescue remains forbidden.

## Unicorn Precision Scalper lane

- Goal: complete one bounded pre-outcome structural refinement and obtain an
  artifact-bound Model-0 verdict without live execution or post-hoc rescue.
- Current state: terminal `KILL`. Four-bar `HYP-UPSC-XAU-M5-002` failed first;
  independently frozen event-anchored HYP-005 passed probe/build but was parked
  for role/cost-tier incompatibility. Identity-only research-control
  `HYP-UPS-XAU-M5-006` completed valid run `20260716_141244`.
- HYP-006 evidence: N=130, 1.2569 trades/week, report PF 0.7242/net
  -4,396.90 USD, research full-cost PF 0.4982, x1.5 0.4129, x2 0.3433,
  robustness 0%, MC P95 DD 7.1176%, equity REJECT. Compile and exact-source
  non-repaint PASS; telemetry 130 OPEN/130 CLOSE with zero zero-risk rows.
- Strategy conclusion: event anchoring was worse than the four-bar control.
  Close the sweep-expiry family; no hour/day/sweep-age/RR/session/score rescue.
- Storage: portable install/data/tester remained on D. Protected C Common
  before/after stayed exactly 137 files / 20,008,308 bytes with identical
  metadata hash; zero C files were created or deleted.
- Owner's improvement request opened fresh execution hypothesis
  `HYP-UPS-XAU-M5-007`, not a sweep-expiry or subgroup rescue. The report-prior
  change is a resting limit at FVG CE, 3-bar expiry, no market fallback; all
  HYP-006 signal/risk thresholds remain unchanged.
- HYP-007 result: `KILL_AT_FILL_FEASIBILITY_PROBE`; 115/251 CE limits filled
  within 3 bars (45.82%), 1.110/week, 87 long/28 short. Fill rate, cadence and
  short-count gates failed. No PnL, source modification or Tester run occurred.
- Storage: D-portable read-only probe; C file count/bytes remained exactly
  unchanged. Workflow terminal had 0 positions/0 orders and was stopped.
- Post-kill engineering hardening is complete on the canonical Unicorn package:
  the source is an explicit `POST_KILL_HARDENED_NO_RUN_AUTHORITY` alert-only
  kernel with fail-closed execution authority, stale-attach protection, an
  execution FSM, strict position ownership, broker/order/freeze guards,
  declared-cost sizing, position-lifecycle risk counters, persistent peak
  equity, actual-fill initial risk and bounded reject telemetry. Verification is
  30/30 tests, MetaEditor 0 errors/0 warnings and exact-source non-repaint PASS
  at source SHA256
  `0B5F272AD849E281B85F574351C718BC554D6BE49B2DC7B6A274855665E94E30`.
  The alert-only runtime smoke was rejected before terminal launch because a
  fresh contract receipt was absent; no bypass/run/metric occurred, MT5 is
  stopped, and C Common remained exactly 137 files / 20,008,308 bytes. This
  changes engineering safety only and does not reopen HYP-006/007 or alter the
  separate frozen HYP-008 RR15 package.
- Closeout validators: candidate registry PASS (counts: registry validator).
  Workspace source-of-truth remains `PARTIAL/RED` only because the declared
  `G:` backup root is unavailable and 10 backup-only files cannot be verified;
  the gate was not weakened or reclassified.
- HYP-008 result: terminal `KILL_DIAGNOSTIC`, run `20260716_144508`.
  N=132 / 1.2762 per week / WR 35.606% / report PF 0.6971 / net
  -$4,904.75 / DD 5.512%; full-cost PF 0.4753, x1.5 0.3908, x2 0.3216.
  Robustness 0/7, MC P95 DD 7.315%, equity REJECT. The WR gain over 2.5R
  was only 0.991 pp while PF/net/DD worsened.
- HYP-008 validation: exact one-change contract 4/4, compile 0/0,
  snapshot-bound non-repaint PASS, 132 OPEN/132 CLOSE and zero zero-risk
  rows. Protected C Common was metadata-identical before/after; zero files
  needed deletion. No live, promotion, rerun or subsequent RR/filter sweep.
- Next economic action: none for the terminal Unicorn hypotheses. The only open
  non-economic gate is independent outcome-blind review of the source-bound
  V1.3 casebook plus a separately frozen agreement/outcome analysis plan.
- Fresh research closeout: the Owner-requested Grok 4.5 runner completed with
  exit 0, a non-empty response and `NO_LEGAL_CANDIDATE`; primary-source checks
  agreed that COT is too slow for M5 and relevant COMEX order-flow/depth is not
  currently licensed. Canonical v1.21 therefore adds only
  `ALERT_FIRST_CASEBOOK_V1`: default-off, alert-only, D-data-path-only, bounded
  to 200 pre-outcome rows and forbidden from writing outcome fields. A separate
  metadata CSV binds source-contract/run/broker/data-path/offset/input identity;
  sweep age now matches the frozen probe. New-bar/history/risk state and broker
  result handling are fail-closed, pending same-symbol exposure is blocked and
  spread is rechecked before send. Lifecycle telemetry defaults off in the safe
  preset. Verification is 41/41 tests,
  compile 0 errors/0 warnings and exact-source non-repaint PASS at source SHA
  `DF86D6CDB00B28CBAFD7FC9540497C60EDE8CD922F9B54470BB8AAD14D11C166`.
  No Tester run, new trading hypothesis, live attach or performance claim was
  created. Next legal gate is at least 100 sealed human labels plus a separate
  pre-outcome analysis plan. A rejected direct-Python nonportable probe created
  360,407,524 bytes on C; the traced run-owned profile was deleted, protected C
  Common remained metadata-identical and terminal count is zero.

- Current engineering closeout: v1.23 makes mutation tester-only, fails closed
  on position/order/history enumeration errors, reconciles money risk at actual
  fill, uses declared slippage for deviation and binds casebook row/meta to the
  source hash with a blank breaker-validity label. The first V1.3 attempt
  `20260716_154857` correctly failed sidecar validation because AlphaFactory
  serialized a string input as an optimization tuple; a red-green type-aware
  serializer fix was bound into the successful receipt. This does not alter the
  detector or reopen HYP-006/007/008.

## GoldMacroPulse real-yield lane

- Goal: test a materially independent official real-yield information set after
  the PO3/KLR family closed; no live execution and no post-hoc rescue.
- Current state: `HYP-GMP-XAU-M15-REALYIELD-001` is
  `KILL_AT_OFFLINE_PROBE`; no EA source, compile, holdout or Strategy Tester run.
- Evidence: challenger N=270 / 1.726 elapsed-week cadence / PF 0.684 /
  -63.10R / DD 16.87% / 0 positive years, versus matched momentum control PF
  0.606 / -83.10R. Gross challenger PF was only 1.061 before the spread proxy.
- Storage: terminal data path was portable `D:`; all three protected C tester
  roots were metadata-identical before/after, terminal count is zero, and no C
  file needed deletion.
- Next experiment: none for this ID. Require a materially different information
  set and fresh preregistration; no threshold/hour/direction/RR/hold rescue.

## Public SDR FX-options feasibility

- Goal: determine whether a free public transaction-level options source can
  support a fresh point-in-time FX hypothesis before any outcome join or EA.
- DTCC state: `FAIL_7_YEAR_ACCESS`; current 110-field files are public, but
  pre-mid-2024 objects are deep-archived without a public restore path.
- CME state: `FAIL_TEMPORAL_CONTINUITY`; official FTP sample is hash-bound at
  252 files / 2,592,226 bytes on `D:`. One-trade-max-per-day cadence is
  4.361/week train and 1.944/week validation; sampled 2023 major-FX new-option
  days are zero. Acquisition/profile verification is 8/8 PASS.
- No price outcomes, hypothesis, `.mq5`, compile, Strategy Tester, holdout or
  live action was authorized. GOAL remains unmet.
- Next experiment: require a materially different source with continuous
  train/holdout coverage. Paid Databento remains blocked pending an Owner key
  and explicit USD ceiling; free SDR rescue is prohibited.

## CME daily futures-participation lane

- Goal: test whether prior-published daily CME FX futures open-interest
  expansion identifies a forced participation flow distinct from the killed
  price-only and weekly COT families.
- Source: 1,763 official CME daily-volume XLSX files, 122,190,328 bytes on
  `D:`, covering 2017-01-03 through 2023-12-29. Exact EC/BP/J1 extraction
  produced 5,289 rows with zero source failures; source tests are 7/7 PASS.
- Current state: `HYP-CME-OI-CONT-H1-FX-001` is terminal
  `KILL_AT_OFFLINE_PROBE`; registry validation passed at closeout
  (counts: registry validator). Train PF gross/x1/x1.5/x2 was 1.121/0.878/0.787/0.706;
  validation was 1.069/0.924/0.854/0.788. Net x1 was -43.01R/-11.95R.
- The candidate missed its frozen +0.10 PF margin over the matched price-only
  control and passed only 15/26 gates. The 2024-2025 sealed holdout remained
  unopened. No `.mq5`, compile, Model 0, live action or parameter rescue is
  authorized.
- Storage: all four protected C roots were metadata-identical before/after;
  terminal count was zero and all retained source/evidence is on `D:`.
