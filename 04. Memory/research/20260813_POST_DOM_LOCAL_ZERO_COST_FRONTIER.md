# Post-DOM local zero-cost XAU/Forex frontier

Date: 2026-08-13

Verdict: `NO_CANDIDATE_LOCAL_ZERO_COST_FRONTIER`

## First unmet gate

There is no zero-cost source currently present in the local MT5/workspace tree
that simultaneously has:

1. a defensible 2018-latest historical/live identity;
2. fields known at a closed M5/M15 decision;
3. a causal signed XAUUSD/major-FX mechanism before outcomes; and
4. an API, venue, payload and mechanism outside the frozen failure catalog.

This is a scoped source-frontier result, not proof that no EA edge exists.

## Local evidence boundary

- AlphaFactory runs catalog: 499 indexed run rows across 143 EA identities;
  zero rows pass all promotion gates.
- Canonical shelf: 107 MQ5 packages; the 16 without a same-name physical run
  were already classified as non-strategy probes, exact terminal objects or
  superseded engineering identities. No lawful revival candidate survived.
- Native Calendar historical/delta paths are terminal under
  `KILL_CALENDAR_LANE`.
- FivePercent live DOM is engineering-readable but terminal under
  `KILL_FIVEPERCENT_DOM_LADDER__NO_HYPOTHESIS_AUTHORITY`.
- Remaining local bar/tick/indicator/session/fix/quote objects de-duplicate to
  price geometry, quote activity or other already-closed families.
- Local deal fixtures, reports and historical ledgers are outcomes/execution
  evidence, not a new pre-trade information source.
- BTC/crypto, paid/licensed data and unapproved source acquisition are outside
  Owner scope.

## Grok Build bounded pass

After being given the API + venue + payload + mechanism de-dup gate, Grok Build
returned `B) NO_CANDIDATE_LOCAL_ZERO_COST_FRONTIER`. Its first unmet gate was
the same: no already-local zero-cost source has defensible 2018-latest
historical/live identity and a new closed-M5/M15 signed payload. Grok proposed no
code, purchase, outcome scan or threshold recombination. Local evidence remains
authority.

## Reopen condition

Reopen discovery only when a materially new file/API becomes available whose
fields can be inspected outcome-blind and whose source identity is outside HCC
bars, quote ticks, the FivePercent DOM ladder, MetaQuotes Calendar and the
already-closed CME/public-macro families. Do not create another collector on the
same APIs to simulate progress.

The overall EA goal remains `ACTIVE / UNMET`; no hypothesis, backtest,
optimization, validation, paper trade or live trade was opened here.
