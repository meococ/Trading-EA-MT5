# Free-official external and lineage-aware XAU/Forex frontier

Date: 2026-08-13

Verdict: `NO_CANDIDATE_FREE_OFFICIAL_EXTERNAL_FRONTIER`

## Scope

This pass continued the active XAU/Forex EA goal after the already-local
zero-cost frontier closed. It allowed one bounded expansion to an external,
zero-cost, official primary source that could be lawfully archived, plus a
lineage-aware audit of existing AlphaFactory runs. It did not authorize source
spend, outcome mining, MQL5, MT5 economics, paper trading or live trading.

## Pre-search de-dup correction

The proposed CPI/BLS/NFP route was rejected before any web probe, source
download or price outcome. The failure catalog already seals:

- the BLS/BEA/Census first-vintage principal-statistics panel (`#14`);
- the X5 principal-statistics densification/rebrand;
- ALFRED/FRED surprise panels and HF macro nowcasts; and
- the MetaQuotes Economic Calendar lane.

CPI/BLS therefore cannot be presented as a new source merely by shrinking the
publisher panel or changing the release name.

## Grok Build bounded result

Grok Build received the full API + publisher/venue + payload + mechanism
de-dup exclusions and returned:

`B) NO_CANDIDATE_FREE_OFFICIAL_EXTERNAL_FRONTIER`

The first unmet gate was a pre-declared signed mechanism expected to remain on
the first or second closed M5/M15 bar after realistic retail spread,
commission and slippage. Official UTC or timestamped lookalikes such as USGS
ComCat, ECB eligible-assets/TARGET aggregates, LBMA/WGC/FRED gold series and
central-bank liquidity/rate series either lack that signed 5-15 minute impulse
or de-duplicate to already-closed price, fix, macro, liquidity or legal/event
families. Grok proposed no source purchase, hypothesis, code or outcome scan.

This is an advisory result. Local failure-catalog and artifact evidence remain
the decision authority.

## Existing-run lineage audit

The AlphaFactory catalog still has no run that passes every gate. Headline
partial passes cannot be composed into a valid multi-sleeve EA:

- `EA_LondonNY` is a sparse sleeve and already failed transfer/flat-period
  review;
- `EA_ITSM`, `EA_Cobra`, `EA_SilverBullet` and `EA_ChopRegime` retain their
  source, contamination, cost, OOS or transfer terminal verdicts;
- `EA_EventAggressorFlow` is terminal and uses a sub-M5 decision clock;
- `EA_CompBreak_XAU_M15_V2` has only 28 trades in its visible baseline and no
  promotion evidence; and
- the runs index is discovery evidence only, not authority to select a family
  from observed PF or profit.

`MULTI_SLEEVE_REQUIRED` remains an architecture rule. It does not permit P&L
pooling, headline-PF selection or revival of terminal sleeves.

## Failure radius and reopen condition

The exact closed radius is: external, zero-cost, official-primary archives for
XAUUSD or major FX, mapped to the first or second closed M5/M15 bar, with
2018-latest first-public/vintage identity and a new signed mechanism after all
sealed price, quote, DOM, Calendar, macro, fix, flow, CME and public-event
families are removed.

Reopen only when either:

1. a materially new official primary payload appears with a defensible signed
   5-15 minute XAU/FX mechanism and historical/live identity; or
2. an existing preregistered sleeve has fresh, independent artifact evidence
   that cures its exact source/cost/OOS failure without changing logic after
   outcomes.

The overall goal remains `ACTIVE / UNMET`. No hypothesis ID, source download,
spend, EA build, backtest, optimization, validation, paper trade or live trade
was opened by this pass.
