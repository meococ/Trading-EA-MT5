# Post-Calendar local/database-first frontier reconciliation

Date: 2026-08-13

Verdict: `NO_LAWFUL_LOCAL_FRONTIER`

## Scope

After the terminal MetaQuotes-Demo Calendar child, Lead and Grok Build screened
only the already-local, zero-purchase frontier. No target outcomes, prices,
run-PF ranking, code generation, backtest, paid source or new hypothesis was
opened.

## Calendar closeout

`HYP-CALENDAR-PIT-MQDEMO-001` compiled with 0 errors/0 warnings and discovered
the full eight-currency catalog, but its first future-window
`CalendarValueHistoryByEvent` call returned `n=-1`, error 5401 with no
occurrence or proof. MetaQuotes defines 5401 as `ERR_CALENDAR_TIMEOUT`, request
time limit exceeded. The frozen zero-error gate therefore kills the child.

Two further same-error timer callbacks occurred while a LiveUpdate modal
delayed shutdown. The disclosed `stop_after_first_fatal=false` deviation is not
a second attachment, source revision or economic exposure and cannot rescue the
failed first call.

## Rejected false next frontier

Grok first suggested a 596,141-row T2/Volman native-M5 engineering restart.
Lead rejected it against newer local authority:
`20260813_T2_P3_ENGINEERING_CLOSEOUT.md` records
`TERMINAL_T2_P3_DUPLICATE_IDENTITY_POPULATION` after the sole authorized full
replay found 11 duplicate normalized PBP identities. A second replay, key/
population repair or continuation under the same object is explicitly
forbidden.

The stale campaign status was corrected to point at that terminal receipt.
Grok then returned `NO_LAWFUL_LOCAL_FRONTIER`: remaining artifacts are closed
families, data/collectors without an unused mechanism, or engineering plumbing
without an admissible information object. The 482-run database remains a
discovery/de-dup index and cannot choose a new EA from PF rows.

## Current boundary

- Do not start a new ID from Calendar, T2/P3/Volman, Cobra, SonicR-448, D1
  TSMOM, CFTC/CME OI/depth, tick/DOM/volume, or another renamed local family.
- Do not backtest or optimize an old row because it looks profitable.
- Do not restart official zero-cost discovery already sealed today.
- `economics_authorized=false`, `live_allowed=false`, and no promotion claim.
- A next executable lane requires a genuinely new Owner/Lead contract naming a
  source/mechanism outside the closed list, or explicit authority for a source
  class currently outside the zero-purchase boundary.

The overall EA goal remains `ACTIVE / UNMET`. This receipt records the lawful
frontier; it is not a claim that profitable EAs are impossible in principle.
