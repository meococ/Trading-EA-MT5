# Independent-cure engineering-boundary audit — 2026-08-13

## Scope

This is a local, outcome-blind lineage audit after
`NO_CANDIDATE_POST_T2_ZERO_COST_FRONTIER`. It asks only whether an existing
XAU/Forex M5/M15 mechanism stopped before economics because of a curable
implementation defect and has no economically evaluated descendant or
equivalent. It authorizes no code change, MT5 run, outcome access, optimization,
source purchase, promotion or live use.

## Local inventory

- `alpha.ps1 list` exposes 107 EA packages.
- The AlphaFactory runs catalog contains 477 indexed runs across 126 EA names;
  zero of 477 pass every `PROP_READY` gate.
- The candidate registry resolves to 379 latest hypothesis IDs. A mechanical
  leaf/pre-economic filter returned 149 rows, but this set contains stale,
  superseded, capability-only and already closed records; row state alone is
  not reopening authority.

## Audited shortlist and lineage result

| Object | Local result |
|---|---|
| BKSR XAUUSD M5-001 / M15-002 | Both fail the same frozen exact-clock coverage gate at 96.5654% versus 97%; the fresh timing mapping did not cure it. |
| STBS / Supertrend | Terminal long harness campaign; the generic native-indicator transition family is closed. |
| CBRK | The distinct compression-break baseline reached economics and failed PF/cadence; its later cost-metadata issue cannot rescue fatal base gates. |
| VRAS USDJPY Asian OU | HYP001/002 engineering and provenance issues have an exact HYP003 economic successor. HYP003 ran Model 0 and was killed at 3/3 losses, PF 0.0 and 0.0115 trades/week. Its lifecycle telemetry defect blocks repricing only and cannot create wins or cadence. |
| Round Cascade | Source-pass lineage continues through HYP011 economic KILL. |
| EURFXOFI | Source-pass lineage continues through HYP016 terminal economics. |
| JCDR | Source-pass lineage continues through HYP006 economic KILL. |
| EventAggressorFlow | HYP013 is economically sealed; concentration failed and its timing is sub-M5. |
| DRAT / EUVIX / older VRAS | Each engineering stop has a corrected economic successor, which is terminal. |
| T2 P3 | Duplicate normalized identity population is a frozen semantic boundary, not an implementation-only repair. |

Canonical VRAS evidence:

- `03. EA Developer/EA_VRAS_RegimeAdaptiveScalperV4/research/HYP-VRAS-USDJPY-M5-003_FAILURE_PACKET.json`
- `03. EA Developer/EA_VRAS_RegimeAdaptiveScalperV4/research/HYP-VRAS-USDJPY-M5-003_KILLED_ROW.json`
- `04. Memory/do_not_repeat_failures.md` under `VRAS USDJPY Asian OU Model 0 economic and lifecycle kill`.

## Grok Build challenge

Grok Build returned `NO_REOPENABLE_ENGINEERING_BOUNDARY`. Its sandbox could not
read the local AlphaFactory artifacts, so that response is advisory only. The
verdict below comes from the local lineage and failure artifacts, not from the
Grok response.

## Verdict

`NO_REOPENABLE_ENGINEERING_BOUNDARY`

The first unmet gate is proof of one exact implementation-only pre-economic
failure whose mechanism has no descendant or equivalent economic evaluation.
None of the audited leaves satisfies both halves. Repairing telemetry on an
already economically killed object is infrastructure work, not an edge or a
rerun authorization.

This is scoped only to the audited existing XAU/Forex M5/M15 inventory. The
overall EA goal remains `ACTIVE / UNMET`. Reopen this boundary only when exact
local artifacts prove: (1) a deterministic implementation defect, (2) zero
economic readout for that same market object, (3) no equivalent economic
successor, and (4) one frozen outcome-blind parity/source probe. Otherwise the
next market attempt requires a materially new source/object within Owner scope.
