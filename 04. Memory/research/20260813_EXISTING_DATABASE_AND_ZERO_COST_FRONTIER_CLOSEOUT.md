# Existing-database and zero-cost frontier closeout

Date: 2026-08-13

Authority: outcome-aware reconciliation and source-capability screening only.
This receipt authorizes no historical survivor, hypothesis, compile, backtest,
paper trade, live trade, purchase or promotion.

## Owner direction and frozen scope

- Continue the active XAUUSD/seven-major-FX goal by exploiting existing code,
  the AlphaFactory database, the FivePercent MT5 setup and bounded Grok Build
  review before inventing another strategy.
- Allowed mechanism horizons remain M5, M15, H1, H4 and D1 when frozen before
  source/outcome access. Weekend holding remains forbidden.
- Historical headlines are discovery pointers only. Engineering validity,
  economic validity and promotion readiness remain separate gates.
- No Git work and no paid data/service were authorized in this loop.

## SonicR v10 evidence recovery

The bounded Grok Build recovery returned
`ORIGINAL_V10_NOT_REPRODUCIBLE_FROM_WORKSPACE`. Current Python modules,
summary/comparison JSON, aggregate walk-forward JSON, full-sample trade JSON and
an XAU yfinance cache exist in Grok's workspace. The exact per-trade OOS tape,
saved fold/embargo assignment, original frozen pack/docs, timezone/session plus
same-bar SL/TP contract and XAU cache-as-run binding do not.

The first fatal gap is inability to bind the claimed PF/sample size to exact
OOS trades. Grok hash-reported an 8,803-byte `MISSING_EVIDENCE.md` with SHA256
`A95E28F0037BD89D8124E48E52B3D5B24EC8ED6E3262ADE96100C6A9679D6466`,
but Grok failed when asked to attach it. It is a remote receipt, not a locally
possessed artifact. No reconstruction, market outcome or local run followed.

## AlphaFactory database reconciliation

The catalog was rebuilt from local run artifacts:

- 499 runs indexed;
- 123 folders skipped;
- 0 indexing errors.

The broad discovery query `PF > 1.30 AND DD < 8 AND trades >= 200` returned 50
rows. The M5/M15 entries remain the already-terminal Cobra, ITSM, Gotobi,
ChopRegime, ShanghaiFixScalp, VolCluster, M15SparkAsian and SilverBullet
families. The extra M1 EventAggressorFlow row is outside the active horizons;
the H1 custom-symbol MultiAssetTSMOM headline is engineering-invalid and its
corrected native result is already terminal. No H1, H4 or D1 survivor was added
by the multi-horizon reset.

Specific Sonic and existing-code checks confirm the same boundary:

- local `EA_SonicR` XAUUSD M5 has a 2024-2025 seed at 127 trades/PF 1.413, but
  its longer 2021-2025 route is 335 trades/PF 1.160 and has no WFA, Monte Carlo
  or robustness receipt;
- old Cobra's best database headline is outcome/trial-selected and does not
  establish source/PIT cleanliness or FivePercent symbol portability;
- SilverBullet, ITSM, ChopRegime, Gotobi, Spark and InsideBar retain their
  documented transfer, untouched-OOS, cadence or Model-0 failures.

Grok independently returned `NO_FORWARD_CANDIDATE`: starting a new timestamp
after selecting the least-dead historical object would not create a clean
information object. Lead concurs. No old EA was restored or paper-traded.

## Existing prospective collectors

- `EA_ProspectiveCalendarPIT` remains `KILL_CALENDAR_LANE`. The sole authorized
  History successor also returned `api_error=5401` on the first two bounded
  future-window calls, with zero accepted occurrences or paired idle proofs.
- `EA_ProspectiveDOMTape` remains
  `KILL_FIVEPERCENT_DOM_LADDER__NO_HYPOTHESIS_AUTHORITY`. Its collector is
  engineering-valid, but nearly every level is the fixed 100,000,000 size
  sentinel and the outer ladder is dealer-rendered/fixed; no independent size
  or flow object remains.
- `EA_PTR_T2_DataEpochD0V3` is a no-trade historical series/Model-4 data-epoch
  probe, not a prospective signal source. It cannot manufacture a mechanism.

No continuous collector or trading EA was started.

## Bounded free-source screen

After receiving the database/collector exclusions, Grok Build returned
`NO_ZERO_COST_SOURCE_CANDIDATE`. Its nearest miss was the already-closed CFTC
COT/TFF cell: public dates identify report dates and revised history, not a
complete historical first-public vintage paired to the live release. Other
nearby official cells failed the same previously recorded original-print,
expectation, licensed-data, revision-clock or mechanical-sign boundary.

This Grok search is advisory. The local decision rests on the existing source
and failure receipts plus the absence of a newly evidenced payload contract;
it does not elevate Grok's source count into authority.

## Verdict and next lawful move

- Verdicts: `NO_LAWFUL_DATABASE_SURVIVOR`, `NO_FORWARD_CANDIDATE` and
  `NO_ZERO_COST_SOURCE_CANDIDATE`.
- Goal: `ACTIVE / UNMET`.
- Active mechanism: none.
- New MQL5, compile, backtest, price outcome, validation, holdout, paper/live
  trade and spend: all zero.
- Reopen only from a genuinely new official/venue source-intake proof, or from
  an explicit Owner scope expansion such as a new executable venue/source or
  paid research-grade data. A purchase remains forbidden without Owner
  approval.
