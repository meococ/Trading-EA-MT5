# Post Event-Depth multi-horizon source frontier

Date: 2026-08-13

## Trigger

HYP009 closed `KILL_FROZEN_MAPPING` after a valid EURUSD Model-0
PRIMARY/REVERSE pair. PRIMARY retained gross directional information but complete
cost erased it. The next loop was required to use a materially independent
information mechanism, not a filter or timing rescue.

## Grok Deep Research passes

The existing signed-in Grok conversation was reused through the in-app browser and
the `/deep-research-trading-meta5` skill.

1. A fresh-mechanism pass returned `NO_CANDIDATE`. It found no distinct
   deterministic source object below USD 10 with a fixed causal side, cost-tolerant
   horizon, adequate 2018-current sample and separation from the closed families.
2. Lead rejected the first answer as potentially anchored to microstructure and
   reran the search under the active XAU/Forex-only M5-D1 scope. This second pass
   also returned `NO_CANDIDATE` after checking:
   - ECB FX intervention: zero usable events in 2018-current;
   - Japan MoF/BoJ intervention: mechanically signed but too sparse and clustered
     for independent TRAIN/OOS evidence;
   - Federal Reserve SOMA/FX operations: essentially absent in-period;
   - central-bank USD swap-line drawdowns: sparse/crisis-clustered and no fixed
     spot side at a preregisterable horizon;
   - other G10 auctions/allotments/reserve flows: missing PIT historical/live
     identity or collapsing into closed rate/yield/positioning families;
   - official derivatives settlement/delivery surfaces: no mechanically fixed
     XAU/FX side with an adequate independent sample.
3. Lead rejected an official-flow-only search boundary and ran a third pass over
   forced-flow and exogenous non-price objects. It again returned `NO_CANDIDATE`
   after checking:
   - index reconstitution and currency-hedge rebalancing: the mechanism is
     documented, but the signed portfolio demand and PIT weights are proprietary;
   - sovereign auction allotment/tail surprises: the channel is documented, but a
     versioned 2018-current pre-release expectation plus first-public result tape
     with exact availability clocks was not available below USD 10;
   - gold/FX ETF primary-market flows: no full-sample PIT creation/redemption tape
     with matching live identity, and the adjacent GLD object is already closed;
   - CLS settlement imbalance: commercial data without a sub-USD-10 historical
     plus live route;
   - other exchange margin/settlement notices: no unique mechanically signed
     XAU/FX mapping with adequate independent cadence.

Grok is advisory. Local de-dup was checked against the candidate registry,
`do_not_repeat_failures.md` and `STRATEGY_LOG.md`. The local frontier already closes
the same official auction/intervention/warehouse, central-bank text, liquidity,
rate/carry, positioning, option, fixing, settlement and source-identity routes.

## Verdict and authority boundary

Verdict: `NO_CANDIDATE_CURRENT_XAU_FX_SOURCE_FRONTIER`.

No new hypothesis, source acquisition, target outcome, MQL5 file, MT5 backtest,
validation/holdout read, optimization or deployment was opened from the three Grok
answers. No additional spend occurred.

This is a scoped frontier verdict, not a claim that no trading edge exists. Reopen
only when a materially new source provides all of: retainable PIT 2018-current
history plus live equivalent, mechanically fixed sign, cost-coherent horizon,
adequate independent observations and a failure radius not already represented in
the registry. The standing Owner authorization below USD 10 remains available for
such a concrete in-scope pilot; it does not justify manufacturing a candidate.

The smallest currently identified capability gap is a versioned archive for major
US Treasury or G10 sovereign auctions containing the exact pre-release consensus
or official expected quantity, first-public allotment/bid-to-cover/stop-out fields,
precise UTC publication timestamp, and a matching live endpoint. This observation
does not open an auction hypothesis or authorize target-return inspection.

Grok conversation:
`https://grok.com/c/b01a3ade-c805-45b9-807e-b7553d51dea2`
