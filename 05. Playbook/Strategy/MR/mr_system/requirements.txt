pandas>=2.0
numpy>=1.24
pyyaml>=6.0
streamlit>=1.37        # cần >=1.37 cho @st.fragment(run_every=...)
plotly>=5.18
# --- chỉ trên Windows, cho mode mt5 ---
MetaTrader5>=5.0.45 ; sys_platform == 'win32'
# --- Phase 1-2 (backtest/validation) sẽ thêm: ---
# arch>=6.0            # VarianceRatio + SPA/RealityCheck/StepM/MCS
# skfolio              # CombinatorialPurgedCV
# quantstats           # reporting + smart_sharpe (Lo 2002)
