# Readout — HYP-REPLACE-ME

## Bound identity

- Registry row SHA256:
- Prereg SHA256:
- Source / capability-contract SHA256:
- Task packet / execution receipt SHA256:
- Run ID, role, symbol, timeframe, window and Model:

## Logic fidelity

- Logic-to-code matrix path/SHA and coverage:
- Ordered state machine and exact entry authorization event:
- Context versus qualification versus trigger roles:
- Invalidation, stop/target, management and restart behavior:
- Material proxies or missing report requirements:

## Evidence quality

- Compile and non-repaint audit:
- Lifecycle sidecar and report reconciliation:
- Cost provenance: `VERIFIED|UNVERIFIED`
- Train / holdout / matched-control identity:

## Results

Report trades, elapsed-calendar-week cadence, PF and expectancy at x1/x1.5/x2
verified cost, DD/tail, stability, concentration and execution quality.

Break down the result by year/half-year/month where available, session,
direction, regime proxy and signal/context state. Rejected opportunities count
as declared by the preregistered opportunity metric.

## Log and execution diagnosis

- Report ↔ lifecycle ↔ RunMeta reconciliation:
- Standard log-triage result and disposition of every material hit:
- Funnel/rejection counts, OrderCheck/send/fill/stop/volume anomalies:
- Holding time, spread/slippage sensitivity and restart-safe accounting:

## Chart casebook and logic diagnosis

For runs with trades, render at least two winners and two losers when available.
Every anatomy chart shows entry, initial SL, TP, actual exit, the configured HTF
context with the entry candle centered, and explicitly labeled post-entry bars.
For zero-trade runs, render representative rejected candidates instead.

- Why winning cases worked:
- Why losing cases failed:
- Which intended logic is missing, too coarse or internally conflicting:
- Which observations are post-outcome leads and therefore forbidden as direct
  rule patches:

## Gate table and verdict

Every frozen gate gets `PASS|FAIL|INSUFFICIENT`. Final research verdict is one
of `SCREENED`, `CHALLENGER`, `CONFIRMED`, `PARKED`, `KILLED`; it never implies
live or funded readiness.

## Next legal action

Append one registry transition. A failed hypothesis may be parked/killed only;
do not tune or rescue it from this readout.

## Delivery gate

- EA delivery packet path/SHA:
- `alpha.ps1 delivery -Packet ...` result:
- Any non-`COMPLETE` analysis dimension and its explicit limitation:
