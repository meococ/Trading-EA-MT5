# AlphaFactory runtime dependencies.
#
# Replaces the 275-line pip freeze that was here until 2026-08-31. That freeze
# pinned things nothing in this repo imports (altair, bottle, chromadb,
# pyinstaller) while burying the handful that actually matter. The old file is
# kept at 00. Old File/alpha_archive/requirements.frozen-275.txt.
#
# Verify with:  & ".\02. AlphaFactory\alpha.ps1" validate
# Python 3.12 (Windows: install user-scope, do NOT rely on the Microsoft Store
# stub in %LOCALAPPDATA%\Microsoft\WindowsApps).

# --- required: alpha.ps1 validate refuses to pass without these -------------
numpy
pandas
matplotlib

# --- required by the live MT5 connector and session_trader -----------------
MetaTrader5        # always attach via tools/factory_paths.mt5_initialize_kwargs()
pydantic           # session_trader/models.py typed artifacts
jsonschema         # tools/execution_data_foundation.py contract validation

# --- required by the test suite --------------------------------------------
pytest
pyarrow            # parquet engine for research_kit / probe_sdk fixtures
openpyxl           # xlsx source samples in the CME participation extractor

# --- optional --------------------------------------------------------------
# vectorbt         # only `alpha.ps1 scan`; heavy, pins old numpy. Install ad hoc.
