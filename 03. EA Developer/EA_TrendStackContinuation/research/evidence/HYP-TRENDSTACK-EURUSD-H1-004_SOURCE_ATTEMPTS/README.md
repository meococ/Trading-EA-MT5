# HYP-004 source attempts

Immutable parent for packet-bound HYP-004 corrected-footer source attempts.
Each child name is the exact `source_attempt_id`; successful creation of its
`attempt_started.json` consumes that ID. Never delete, overwrite, resume or
reuse a child, marker, failed stage or terminal record.

This parent grants no price, economic, validation, holdout, MT5 or trading
authority.
