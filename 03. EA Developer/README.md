# 03. EA Developer

Sự thật package = `./02. AlphaFactory/alpha.ps1 list` + contract của EA đang mở.
Graveyard = `00. Old File/` (gitignore; không nguồn compile/evidence).

| Sống | Path | Vai trò |
|---|---|---|
| `EA_SonicR_PVSRA` | `03. EA Developer/EA_SonicR_PVSRA/` | Host Deploy Sonic R |

`EA_SonicR` classic (còn trên shelf và/hoặc `00. Old File/`) **không** compile cho goal.
