# HYP009 audit-governance addendum

Status: frozen after the source/compile preregistration and before any HYP009 packet or MT5 attempt.

This addendum changes no source, signal, ATR, geometry, risk, exit, DESIGN window or acceptance threshold. The original flat-fastpath preregistration remains the source-development authority. This document freezes only the subsequent evidence harness.

- `STBS009-PACKET-BUILD-001` claims durably before any bound registry/artifact read and may create only a data-acquisition packet.
- After a separate independent post-packet review, `STBS009-MODEL0-AUDIT-001` claims durably before any bound receipt read or AlphaFactory invocation.
- The packet/receipt authority is exactly `DATA_ACQUISITION_ONLY_NO_MODEL0_PERFORMANCE`; the registry data-acceptance contract is bound separately from AlphaFactory's exact six-key `binding.data_quality_contract`.
- The screened row may authorize only Model-0 MT5 data acquisition, run-scoped MQL5 compilation, run-artifact collection and the frozen oracle/report comparator inside the same one-shot launcher. Trade API, performance/outcome/economic access, optimization, validation, holdout, paper and live remain false.
- The reviewed static EX5 and compile log are copied byte-exactly into the claimed packet root so the Alpha run cannot overwrite their evidence. The run-scoped EX5/config/compile log are validated and sealed separately.
- The packet seals the Git path set. The post-packet review exists before sealing as a disclosed non-authoritative placeholder; only its bytes may change afterward. Both attempt roots are exact `.gitignore` reservations.
- The sole run is XAUUSD/M15, 2005.01.01–2023.01.01, Model 0, current spread, timeout 300, exact override `InpAuditOnly=true`.
- PASS requires exact dual-axis signal reconciliation: numeric epochs match oracle UTC, printed timestamps match oracle server axis, counts are 690/683/7/339/344 with ATR/geometry 683, and no strategy request/order/deal/entry/close record exists.
- The HTML report may contain exactly one tester-start funding-balance row at `2005.01.01 00:00:00`; no BUY/SELL order or deal is allowed.

No PF, balance movement, return, expectancy, drawdown or market-edge inference is authorized in HYP009.
