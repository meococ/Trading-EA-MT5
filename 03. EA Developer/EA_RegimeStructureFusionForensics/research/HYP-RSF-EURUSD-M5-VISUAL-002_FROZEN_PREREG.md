# HYP-RSF-EURUSD-M5-VISUAL-002 — Frozen native visual replay

## Authority

Diagnostic-only replay of terminal parent `HYP-RSF-EURUSD-M5-BLOCK1-001`,
Cell 16. This hypothesis cannot rescue, tune, optimize, validate, promote, or
reinterpret the parent economic result. It exists only to replace the earlier
synthetic casebook with native MT5 visual evidence.

## Frozen source population

- Symbol/feed: `EURUSD`, AlphaFactory FivePercent tester history.
- Execution chart: `M5`; context engines remain `M15` inside the unchanged EA.
- Model: MT5 Model 0 / real ticks.
- Window: `2018.01.01` through `2022.12.31`.
- Parent trading rules, costs, sessions, risk, stops and targets: unchanged.
- Expected replay identity: 670 positions, net `-5252.60`, PF approximately
  `0.72037`, matching run `20260806_231942` before any image interpretation.

## Preselected chart cases

Exactly 14 positions from the already frozen `selection_manifest.json`:

`342, 424, 782, 808, 500, 1052, 832, 774, 456, 422, 604, 780, 1222, 246`.

The set contains engine/direction loser-winner pairs plus global extreme loss
and win. No other position may produce accepted PNG evidence.

## Visual evidence contract

1. AlphaFactory config must contain `[Tester] Visual=1` and `Optimization=0`.
2. `InpForensicVisualScreenshots=true` and screenshots are allowed only when
   `MQL_VISUAL_MODE` is true.
3. Capture OPEN and final CLOSE after a later tick/redraw so native trade
   markers and indicator plots are painted.
4. Each CSV row binds `case_id`, deal, order, position, event time, price,
   original SL/TP, engine, file name, result, symbol, timeframe and run ID.
5. The run manifest must hash the EA source/EX5, all five indicator
   source/EX5 pairs, visual config, report, visual CSV and every PNG.
6. M5 images cover MBB, TB SMC and QQE. AIRD/VRC M15 context requires a
   separate synchronized native M15 capture lane; M15 state must never be
   visually inferred from an M5-only picture.
7. Any replay mismatch, missing/duplicate image, screenshot error, unbound
   indicator binary or simulated/Brownian chart makes visual evidence invalid.

## Interpretation guardrail

Images are examined as paired loser/winner cases. A visible pattern can only
become a new mechanism after it is written as a closed-bar computable rule and
preregistered under a new hypothesis ID. No threshold may be selected from
these 14 charts and reported as an edge. Population/OOS/cost/DSR/CPCV gates
remain mandatory for any successor EA.

