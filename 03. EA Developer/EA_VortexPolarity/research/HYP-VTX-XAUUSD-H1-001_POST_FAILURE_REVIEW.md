# HYP-VTX-XAUUSD-H1-001 — Post-failure Review

Status: `PASS_PARK`

The sole attempt is complete and non-repeatable. Pooled cadence (`11.1440/week > 5`) and every-year cadence (`10.2603–11.5137/week > 6.5`) failed; all remaining source gates passed.

The failure radius is only pure unscaled Vortex-14 polarity crossover on native XAUUSD H1, 2018–2022. No outcome was opened. Period extension, minimum VI separation, smoothing or another filter would be post-hoc rescue and is prohibited.

Independent review reconciled the complete chain and all 2,907 executable ledger rows, including exact crossover predicates, +1h decisions, counts, years and gate arithmetic. `PASS_PARK` is warranted and means source over-frequency only.

The next lane uses a different event-generating indicator. Supertrend 10/3 direction flips are materially new because the recursive ATR trailing-band state machine emits regime changes rather than every oscillator recross. Its preregistration must seed from native pre-2018 source history, freeze Wilder ATR/update order/initial state, and score only 2018–2022.
