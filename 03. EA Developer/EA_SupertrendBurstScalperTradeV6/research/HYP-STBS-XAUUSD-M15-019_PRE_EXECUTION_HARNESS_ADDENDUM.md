# HYP-STBS-XAUUSD-M15-019 pre-execution harness addendum

This addendum is governance-only. It does not change the frozen HYP019 source, parameters, cost assumptions, baseline thresholds, data window or economic hypothesis, and it opens no MT5 or outcome evidence.

The source package passed the independent static review before the initial screened row. The execution contract is now bound to task packet SHA256 `0EEC152DB8DE5C4132B1AD4B002F11BA37B70A31E25CDBD6771367796446A78D`, initial registry row SHA256 `89B2734185631841D01809F7C12F19F1F0BFD138EDB0B9025E31A99D9A20F2AD`, packet registry SHA256 `C9B9D226BD4F5A7F87998F574752C68C10159B2CF3963E3AA60A7B168E7AF8F2` and packet git-status SHA256 `FE407C25B9A7C9A64625AF108C2647C62B799835A36C8B15CB2638858B2C3D27`.

The successor screened authority must preserve every strategy and acceptance field and add only the reviewed one-shot controls:

- attempt ID `STBS019-MODEL0-TRAIN-001`, limit one, consumed zero before execution;
- exact timeout 900 seconds and exact task-packet path/SHA;
- durable exclusive start claim before AlphaFactory compile/backtest and a COMPLETE or FAILED terminal; a crash consumes the same attempt and forbids retry;
- exact packet-to-registry comparison of the five supplemental baseline fields and the research-proxy permission boundary;
- exact source, preregistration, EA contract, cost manifest, compile, non-repaint, test and independent-review hashes;
- exact runner, AlphaFactory, cost builder, unified validator, registry validator and focused test hashes;
- spread is semantically `current` in packet/receipt/manifest, while the AlphaFactory CLI `-Spread` switch is omitted;
- the authoritative invocation is XAUUSD M15, 2005.01.01 through 2023.01.01, Model 0, ExecutionMode 0, FixedDelay 0, Deposit 100000, Leverage 100, control, trade-only lifecycle-v3 telemetry and the frozen HYP019 overrides.

Engineering validity is evaluated before economics. A nonzero runtime-failure flag, incomplete RunMeta/lifecycle rows, report-to-lifecycle mismatch, orphan/pending exposure, stop-out/emergency exit, wrong fingerprint, trade outside the economic window, run identity drift or compilation/runtime failure invalidates PF and terminalizes this attempt without an economic verdict.

Only a fully reconciled run may build and deterministically replay the research-proxy cost artifact. Optimization, OOS, validation, holdout, economic-validity, promotion, paper and live permissions remain false regardless of the baseline result.
