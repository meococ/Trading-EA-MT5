# Dataset location

The EURUSD H1/H4/M1 parquets referenced by this lane's artifacts now live
at `02. AlphaFactory/data/fivepercent/EURUSD/` (same SHA256, see
`manifest.json` there). `EURUSD_PULL_AUDIT.json` in this folder remains
the acquisition evidence.
