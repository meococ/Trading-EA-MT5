# Public EA verification readout

**Verdict: `INSUFFICIENT_VERIFIED_DATA`.** The frozen F01-F15 cohort contains
15 Grade-C entities and zero performance-eligible Grade-A/B exact-product
accounts, below the mandatory minimum of five. No profit ranking is permitted.

F01's same-author signal failed duration and exact-product attribution because
the account was disclosed as running multiple EAs. F02 had vendor screenshots
without a complete reproducible experiment. F03-F14 remained vendor
descriptions/reviews/claims. F15 stated tester capability but supplied none of
the hash-bound source/binary, preset, data, build, execution, and complete
report package required for Grade B.

Every rejection is preserved in `PUBLIC_EVIDENCE_LEDGER.json`; the local
validator recomputed 0 eligible entities in `PUBLIC_VERIFICATION_READOUT.json`.
Vendor claims remain usable only for the architecture matrix.
