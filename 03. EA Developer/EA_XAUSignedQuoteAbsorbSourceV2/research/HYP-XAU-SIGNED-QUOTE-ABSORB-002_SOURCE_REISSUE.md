# HYP-XAU-SIGNED-QUOTE-ABSORB-002 — Source runtime reissue V2

V1 run `20260812_041329` completed MT5 Model 4 and produced a report, but per-signal journal lines exceeded AlphaFactory's bounded journal-delta evidence. The run was rejected before source-gate acceptance and no economic outcome was authorized.

V2 is a fresh package identity with identical quote classification, signal state machine, thresholds, window, session, symbol and dates. The only source change removes per-signal `PrintFormat`; the final aggregate summary remains. Frozen preregistration remains the V1 package file and the V1 run artifacts/source snapshot remain immutable.

This is an engineering evidence reissue, not a parameter or trading-logic revision.
