# Merge Memo — Hybrid ICT-Sonic build request

## Context

- `memo_id`: `MERGE_MEMO_20260715_HYBRID_ICT_SONIC_BUILD_REQUEST`
- `hypothesis_id`: none registered (request rejected as package)
- `packets_merged`: [red-team session `b6b8d5eb`, research session `189a8b38`]
- `created_at`: 2026-07-15 ~16:40 ICT
- `created_by`: coordinator (parent)

## Receipts

| Role | Verdict | Confidence | Key evidence |
|------|---------|------------|--------------|
| red-team | `KILL_AS_REVIVAL` | high | Full Hybrid stack = Classic Sonic DNA (Dragon+wave+PVSRA+London/NY) + killed ICT/FVG structural class; empty shelf; cost frontier STOP |
| research | `CANDIDATES` (stripped) | med | Proposed `HYP-HYB-ICT-H4FVG-M15-DRAGON-RECLAIM-001` without PVSRA/wave; still uses H4 FVG + Dragon mid reclaim |
| impl | not spawned | — | Write blocked until Owner path + de-dup clear |
| qc | not spawned | — | — |

## Conflicts

- Conflict order applied: gates > quant > systems > trader intuition
- Notes: Research escape hatch still stacks **Dragon** (S360 INVALIDATED — Dragon channel no M15 FX edge) + **FVG** (Structural V3 `HYP-H1-DISPLACE-FVG-CONT-001` KILL; do-not densify FVG%). Red-team wins on full package. Stripped candidate is **not** cleared for GO/impl — at best a future Owner-scoped PROBE with fresh de-dup attestation, not “Hybrid ICT-Sonic” branding.

## Decision (exactly one)

`PARK`

- Rationale (evidence-bound):
  1. Owner brief as written is illegal composite revive under `do_not_repeat_failures.md` + S360 + Structural V3, not a new mechanism.
  2. Active shelf empty; `ea_contract.ps1` fail-closed — compile/backtest from archive invalid.
  3. Research council itself: start manual 50–100 setups; no live; edge only after cost-honest PF proof.
  4. QFSI cost frontier still STOP — cannot claim “PF≥1.65 after slip” as pass gate.
  5. Doctrine: offline probe before EA code; full Hybrid EA scaffold would skip de-dup and burn ceremony on a known-dead stack.

- Next move (Owner must pick one):
  - **A — Manual first (aligned with research §9):** MT5 template + journal Hybrid setups on EURUSD M15; no EA. Coordinator supports settings/journal schema only.
  - **B — New-mechanism PROBE_FIRST:** Owner authorizes a **non-Classic, non-FVG-cont-densify** closed-bar object (new hyp id, not Hybrid ICT-Sonic name). Offline OHLC probe before any shelf package.
  - **C — Explicit override:** Owner writes in-chat that shelf restore + Hybrid mechanical stub is authorized **knowing revive risk**; then coordinator scopes stripped stub + offline probe only (still no Model 0 / no live claim).

- `hot.md` updated: yes
