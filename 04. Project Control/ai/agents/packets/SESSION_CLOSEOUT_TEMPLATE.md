# Session Closeout Template

Copy to `SESSION_CLOSEOUT_<YYYYMMDD>_<topic>.md`. English. Coordinator-owned.
Skill: `04. Project Control/ai/skills/session-closeout/SKILL.md`.
Beats: AGENTS §6 / roster § E — (A) docs (B) self-improve merge (C) cleanup.

## Meta

- `closeout_id`:
- `session_date`:
- `created_at`:
- `owner_scope`:
- Parent drove closeout proactively (did not wait for Owner ask): yes | no

## A. Research / docs update

Touch only what the session actually changed:

| File / class | Needed? | Updated? | Notes |
|--------------|---------|----------|-------|
| `hot.md` | if truth/scope/blocker/next changed | yes \| no \| n/a | |
| `INDEX.md` | if workspace map / canonical path changed | yes \| no \| n/a | |
| doctrine / roster / role / skills | if process changed | yes \| no \| n/a | |
| receipts / manifests | if ceremony or cleanup produced them | yes \| no \| n/a | |
| `do_not_repeat_failures.md` | if new kill / dead-end | yes \| no \| n/a | |
| `GOAL.md` | **only** if Owner decided | yes \| no \| n/a | |
| Other (list): | | | |

Gates red before close: fixed | open (blocker): | n/a

## B. Self-improve + sub proposals → parent merge

- Lessons with evidence (repeated friction / failed call / better procedure):
  - [ ] none
  - [ ] promoted → path(s):
  - [ ] deferred (why):
- Sub proposals received (role + one-liner):
- Parent merge result: merged | rejected | deferred | none
- Standing ops left untouched by subs (propose-only honored): yes | no | n/a
- AGENTS stayed lean (rule + pointer only): yes | n/a

## C. Artifact inventory

| Path / pattern | Class | Action | Notes |
|----------------|-------|--------|-------|
| | keep / archive / delete / scratch | | |

Protected keep (cited by hot / receipt / registry / prereg / readout):

## Cleanup executed

- Dry-run plan path (if any):
- Tools used: inventory | archive_backtest | dedupe | hygiene | other
- Archive destination / manifest (if any):
- Deleted / reclaimed (summary only; no bulk log dump):
- `runs.db` rebuilt: yes | no | n/a

## Residual risks

- Over-delete / AGENTS bloat / skipped docs / unmerged sub proposals:

## Decision

Session closed: `CLEAN` | `PARTIAL` | `BLOCKED`

- Blockers:
- Next move:
